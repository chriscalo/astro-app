{
  "name": "astro-app",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "nodemon -e js,mjs,json,astro,sql --watch src --ignore dist --exec 'astro build && node run-server.mjs'",
    "start": "nodemon --exec 'astro build && node run-server.mjs'",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/node": "^8.2.1",
    "@astrojs/vue": "^4.0.8",
    "astro": "^4.4.4",
    "express": "^4.18.2",
    "vue": "^3.4.19"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
